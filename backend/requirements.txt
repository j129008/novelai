# Web framework and server
fastapi==0.115.6
uvicorn==0.34.0

# Environment variable loading (.env file support)
python-dotenv==1.0.1

# HTTP client for NovelAI API calls
httpx==0.28.1

# Image processing (PNG metadata reading, gallery thumbnails, sketch compositing)
Pillow==12.1.0
numpy==2.4.0
